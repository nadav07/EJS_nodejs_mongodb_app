apiVersion: v1
kind: RoleBinding
metadata:
  name: app-role-binding
  namespace: app
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: app-role
subjects:
  - kind: serviceAccount
    name: app-service-account
    namespace: app
